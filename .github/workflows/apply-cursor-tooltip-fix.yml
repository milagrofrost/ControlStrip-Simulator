name: Apply cursor and tooltip fix

on:
  push:
    branches: [agent/icon-data-urls]

permissions:
  contents: write

jobs:
  apply-fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/icon-data-urls
          fetch-depth: 0
      - run: python3 scripts/apply_cursor_tooltip_fix.py
      - run: npm ci && npm run build
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/ControlStrip.ts src/ControlStrip.css
          git commit -m "Remove pane tooltips and use pointer cursor"
          git push origin HEAD:agent/icon-data-urls
